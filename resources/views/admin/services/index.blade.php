@extends('admin.layout')
@section('content')
<div class="flex flex-wrap items-center justify-between gap-4"><div><h1 class="text-3xl font-extrabold text-[#0a192f]">Services</h1><p class="mt-2 text-sm text-slate-500">Only courier service pages are managed here. Coverage has its own separate module.</p></div><div class="flex gap-3"><a href="{{ route('admin.services.directory.edit') }}" class="rounded-full border border-slate-200 bg-white px-5 py-3 text-sm font-bold">Directory settings</a><a href="{{ route('admin.services.create') }}" class="rounded-full bg-[#0a192f] px-6 py-3 text-sm font-bold text-white">+ Create service</a></div></div>
<form class="flex gap-3 rounded-2xl border border-slate-200 bg-white p-5"><input name="search" aria-label="Search services" value="{{ request('search') }}" placeholder="Search services…" class="rounded-xl border border-slate-200 px-4 py-2 text-sm"><button class="rounded-xl bg-[#0a192f] px-5 py-2 text-sm font-bold text-white">Search</button><a href="{{ route('admin.services.index') }}" class="p-2 text-sm text-slate-500">Reset</a></form>
<div class="overflow-x-auto rounded-2xl border border-slate-200 bg-white"><table class="w-full text-left text-sm"><thead class="bg-slate-50 text-xs uppercase text-slate-500"><tr><th class="p-5">Service</th><th class="p-5">Status</th><th class="p-5">Order</th><th class="p-5">Actions</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($services as $service)<tr><td class="p-5"><a href="{{ route('admin.services.edit', $service) }}" class="font-bold text-[#0a192f]">{{ $service->title }}</a><p class="mt-1 text-xs text-slate-500">/{{ $service->slug }}</p></td><td class="p-5"><span class="rounded-full px-3 py-1 text-xs font-bold {{ $service->status === 'published' ? 'bg-emerald-50 text-emerald-700' : 'bg-amber-50 text-amber-700' }}">{{ ucfirst($service->status) }}</span></td><td class="p-5 text-slate-500">{{ $service->sort_order }}</td><td class="p-5"><div class="flex items-center gap-4"><a class="font-bold text-blue-600" href="{{ route('admin.services.edit', $service) }}">Edit</a>@if($service->status === 'published')<a class="text-slate-600" href="{{ rtrim(config('app.frontend_url'), '/') }}/{{ $service->slug }}" target="_blank" rel="noopener">View</a>@endif<form method="POST" action="{{ route('admin.services.destroy', $service) }}" onsubmit="return confirm('Delete this service and all of its content?');">@csrf @method('DELETE')<button class="text-red-600">Delete</button></form></div></td></tr>@empty<tr><td colspan="4" class="p-8 text-center text-slate-500">No services found.</td></tr>@endforelse</tbody></table></div>
{{ $services->links() }}
@endsection
