@extends('admin.layout')
@section('content')
<div class="flex flex-wrap items-center justify-between gap-4"><div><h1 class="text-3xl font-extrabold text-[#0a192f]">Services & Coverage</h1><p class="mt-2 text-sm text-slate-500">Manage service pages, repeatable sections, coverage content and search metadata.</p></div><a href="{{ route('admin.content-pages.create') }}" class="rounded-full bg-[#0a192f] px-6 py-3 text-sm font-bold text-white">+ Create service</a></div>
<form class="flex gap-3 rounded-2xl border border-slate-200 bg-white p-5"><input name="search" aria-label="Search pages" value="{{ request('search') }}" placeholder="Search services…" class="rounded-xl border border-slate-200 px-4 py-2 text-sm"><button class="rounded-xl bg-[#0a192f] px-5 py-2 text-sm font-bold text-white">Search</button><a href="{{ route('admin.content-pages.index') }}" class="p-2 text-sm text-slate-500">Reset</a></form>
<div class="overflow-x-auto rounded-2xl border border-slate-200 bg-white"><table class="w-full text-left text-sm"><thead class="bg-slate-50 text-xs uppercase text-slate-500"><tr><th class="p-5">Page</th><th class="p-5">Type</th><th class="p-5">Status</th><th class="p-5">Order</th><th class="p-5">Actions</th></tr></thead><tbody class="divide-y divide-slate-100">@foreach($pages as $page)<tr><td class="p-5"><a href="{{ route('admin.content-pages.edit', $page) }}" class="font-bold text-[#0a192f]">{{ $page->title }}</a><p class="mt-1 text-xs text-slate-500">/{{ $page->slug }}</p></td><td class="p-5 text-xs font-bold uppercase text-slate-500">{{ str_replace('_', ' ', $page->page_type) }}</td><td class="p-5"><span class="rounded-full px-3 py-1 text-xs font-bold {{ $page->status === 'published' ? 'bg-emerald-50 text-emerald-700' : 'bg-amber-50 text-amber-700' }}">{{ ucfirst($page->status) }}</span></td><td class="p-5 text-slate-500">{{ $page->sort_order }}</td><td class="p-5"><div class="flex items-center gap-4"><a class="font-bold text-blue-600" href="{{ route('admin.content-pages.edit', $page) }}">Edit</a>@if($page->status === 'published')<a class="text-slate-600" href="{{ rtrim(config('app.frontend_url'), '/') }}/{{ $page->slug }}" target="_blank" rel="noopener">View</a>@endif @if($page->page_type === 'service')<form method="POST" action="{{ route('admin.content-pages.destroy', $page) }}" onsubmit="return confirm('Delete this service and all of its sections?');">@csrf @method('DELETE')<button class="text-red-600">Delete</button></form>@endif</div></td></tr>@endforeach</tbody></table></div>
{{ $pages->links() }}
@endsection
