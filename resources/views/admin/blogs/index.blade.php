@extends('admin.layout')
@section('content')
<div class="flex items-center justify-between gap-4"><div><h1 class="text-3xl font-extrabold text-[#0a192f]">Blogs & SEO</h1><p class="mt-2 text-sm text-slate-500">Write stories, manage your content and control how it appears in search.</p></div><a href="{{ route('admin.blogs.create') }}" class="rounded-full bg-[#0a192f] px-6 py-3 text-sm font-bold text-white">+ Create blog</a></div>
<form class="flex flex-wrap gap-3 rounded-2xl border border-slate-200 bg-white p-5"><input name="search" aria-label="Search blogs" value="{{ request('search') }}" placeholder="Search blog titles…" class="rounded-xl border border-slate-200 px-4 py-2 text-sm"><select name="status" aria-label="Filter by status" class="rounded-xl border border-slate-200 px-4 py-2 text-sm"><option value="">All statuses</option><option value="published" @selected(request('status') === 'published')>Published</option><option value="draft" @selected(request('status') === 'draft')>Draft</option></select><button class="rounded-xl bg-[#0a192f] px-5 py-2 text-sm font-bold text-white">Filter</button><a href="{{ route('admin.blogs.index') }}" class="p-2 text-sm text-slate-500">Reset</a></form>
<div class="overflow-x-auto rounded-2xl border border-slate-200 bg-white"><table class="w-full text-left text-sm"><thead class="bg-slate-50 text-xs uppercase text-slate-500"><tr><th class="p-5">Article</th><th class="p-5">Status</th><th class="p-5">Published</th><th class="p-5">Actions</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($blogs as $blog)<tr><td class="p-5"><div class="flex items-center gap-4">@if($blog->image_path)<img src="{{ Storage::disk('public')->url($blog->image_path) }}" alt="" class="h-14 w-20 rounded-lg object-cover">@endif<div><a href="{{ route('admin.blogs.edit', $blog) }}" class="font-bold text-[#0a192f]">{{ $blog->title }}</a><p class="mt-1 text-xs text-slate-500">{{ $blog->category }} @if($blog->is_featured) · Featured @endif</p></div></div></td><td class="p-5"><span class="rounded-full px-3 py-1 text-xs font-bold {{ $blog->status === 'published' ? 'bg-emerald-50 text-emerald-700' : 'bg-amber-50 text-amber-700' }}">{{ ucfirst($blog->status) }}</span></td><td class="p-5 text-xs text-slate-500">{{ $blog->published_at?->format('d M Y') ?? '—' }}</td><td class="p-5"><div class="flex items-center gap-4"><a class="font-bold text-blue-600" href="{{ route('admin.blogs.edit', $blog) }}">Edit</a>@if($blog->status === 'published')<a href="{{ rtrim(config('app.frontend_url'), '/') }}/blog/{{ $blog->slug }}" target="_blank" rel="noopener" class="text-slate-600">View</a>@endif<form action="{{ route('admin.blogs.destroy', $blog) }}" method="POST" onsubmit="return confirm('Delete this blog? It will be removed from the website.');">@csrf @method('DELETE')<button class="text-red-600">Delete</button></form></div></td></tr>@empty<tr><td colspan="4" class="p-12 text-center text-slate-500">No blogs found. Create your first article to get started.</td></tr>@endforelse</tbody></table></div>
{{ $blogs->links() }}
@endsection
