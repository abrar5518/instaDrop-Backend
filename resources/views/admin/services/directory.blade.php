@extends('admin.layout')
@section('content')
<div><a href="{{ route('admin.services.index') }}" class="text-xs font-bold text-blue-600">← Services</a><h1 class="mt-3 text-3xl font-extrabold text-[#0a192f]">Services directory settings</h1><p class="mt-2 text-sm text-slate-500">Controls only the /services listing page. Individual service content is managed separately.</p></div>
@if($errors->any())<div class="rounded-2xl border border-red-200 bg-red-50 p-5 text-sm text-red-800"><ul class="list-disc pl-5">@foreach($errors->all() as $error)<li>{{ $error }}</li>@endforeach</ul></div>@endif
<form method="POST" action="{{ route('admin.services.directory.update') }}" class="grid gap-6 xl:grid-cols-[1fr_340px]">@csrf @method('PUT')
<div class="space-y-6"><section class="space-y-5 rounded-2xl border border-slate-200 bg-white p-6"><h2 class="text-lg font-extrabold">Directory hero</h2>@foreach([['hero_badge','Badge'],['hero_title','Heading'],['cta_label','Button label'],['cta_url','Button URL']] as [$name,$label])<div><label class="mb-2 block text-xs font-bold">{{ $label }}</label><input name="{{ $name }}" value="{{ old($name, $settings->$name) }}" class="w-full rounded-xl border border-slate-200 px-4 py-3"></div>@endforeach <div><label class="mb-2 block text-xs font-bold">Description</label><textarea name="hero_description" rows="5" class="w-full rounded-xl border border-slate-200 px-4 py-3">{{ old('hero_description', $settings->hero_description) }}</textarea></div></section><section class="space-y-5 rounded-2xl border border-slate-200 bg-white p-6"><h2 class="text-lg font-extrabold">SEO</h2>@foreach([['meta_title','Meta title'],['meta_keywords','Meta keywords'],['og_title','Social title']] as [$name,$label])<div><label class="mb-2 block text-xs font-bold">{{ $label }}</label><input name="{{ $name }}" value="{{ old($name, $settings->$name) }}" class="w-full rounded-xl border border-slate-200 px-4 py-3"></div>@endforeach @foreach([['meta_description','Meta description'],['og_description','Social description']] as [$name,$label])<div><label class="mb-2 block text-xs font-bold">{{ $label }}</label><textarea name="{{ $name }}" rows="3" class="w-full rounded-xl border border-slate-200 px-4 py-3">{{ old($name, $settings->$name) }}</textarea></div>@endforeach <label class="flex gap-2 text-xs"><input type="checkbox" name="noindex" value="1" @checked(old('noindex', $settings->noindex))> Noindex</label><label class="flex gap-2 text-xs"><input type="checkbox" name="nofollow" value="1" @checked(old('nofollow', $settings->nofollow))> Nofollow</label></section></div>
<aside class="xl:sticky xl:top-6"><section class="rounded-2xl border border-slate-200 bg-white p-6"><button class="w-full rounded-full bg-[#c6ff00] px-6 py-3.5 text-sm font-extrabold text-[#0a192f]">Save directory</button></section></aside></form>
@endsection
